# =====================================================
# FILE .env LENGKAP UNTUK REDIS - COPY PASTE INI
# =====================================================
# Baris yang diberi # di depan = HAPUS atau KOMENTARI
# Baris tanpa # = GUNAKAN (copy ke .env Anda)
# =====================================================

# =====================================================
# CACHE CONFIGURATION - UBAH INI
# =====================================================
CACHE_DRIVER=redis
CACHE_STORE=redis
CACHE_PREFIX=ymsofterp_cache

# HAPUS baris di bawah ini jika ada (DUPLIKAT):
# CACHE_STORE=database
# CACHE_PREFIX=

# =====================================================
# SESSION CONFIGURATION - UBAH INI
# =====================================================
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# HAPUS baris di bawah ini jika ada:
# SESSION_DRIVER=database

# =====================================================
# QUEUE CONFIGURATION - OPSIONAL (ubah ke redis)
# =====================================================
QUEUE_CONNECTION=redis

# HAPUS baris di bawah ini jika ada:
# QUEUE_CONNECTION=database

# =====================================================
# REDIS CONFIGURATION - TAMBAHKAN INI
# =====================================================
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_DB=0
REDIS_CACHE_DB=1

# =====================================================
# SETTING LAINNYA (tetap sama, tidak perlu diubah)
# =====================================================
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
MEMCACHED_HOST=127.0.0.1

# =====================================================
# CARA PENGGUNAAN:
# =====================================================
# 1. Buka file .env Anda
# 2. Cari dan ganti bagian berikut:
#    - CACHE_DRIVER, CACHE_STORE, CACHE_PREFIX
#    - SESSION_DRIVER
#    - QUEUE_CONNECTION
#    - Tambahkan REDIS_DB dan REDIS_CACHE_DB
# 3. Hapus atau komentari (#) baris yang duplikat:
#    - CACHE_STORE=database (jika ada duplikat)
#    - SESSION_DRIVER=database (jika ada)
#    - QUEUE_CONNECTION=database (jika ingin pakai redis)
# 4. Setelah update, jalankan:
#    php artisan config:clear
#    php artisan cache:clear
#    php artisan config:cache
# 5. Test dengan: php artisan tinker
#    Lalu ketik: Cache::put('test', 'Hello', 60); Cache::get('test');
# =====================================================
